#!/bin/sh
set -e

if [ "$1" = "remove" ] || [ "$1" = "deconfigure" ]; then
    if [ -d /run/systemd/system ]; then
        systemctl stop sanalu.service >/dev/null 2>&1 || true
        systemctl disable sanalu.service >/dev/null 2>&1 || true
    fi
fi

exit 0
